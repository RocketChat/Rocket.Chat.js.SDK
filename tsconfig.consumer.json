{
	// Reaches the shipped source only by following imports from the entry point,
	// the way a consuming app does, so a missing `/// <reference>` on an untyped
	// dependency fails here.
	"extends": "./tsconfig.json",
	"files": ["index.ts"]
}
